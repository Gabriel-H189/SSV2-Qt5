Seagull Scaring V2 (Qt Edition) install instructions

1. Extract the `media` directory, `requirements.txt`, `seagull.png` and all `*.py` files into a folder you can easily access.
2. Run `install.bat` and type `y`. It's that easy.
3. Run `python main_qt5.py`.
4. Have fun!